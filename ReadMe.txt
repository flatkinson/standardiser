This is a tool designed to provide a simple way of standardising molecules as a prelude to e.g. molecular modelling exercises.

To install, download the project and ensure the standardise/ directory is in your PYTHONPATH.

Note that RDKit (http://www.rdkit.org/) must be installed and accessible to Python.

Please see the IPython Notebooks in docs/ (or the HTML versions in bin/html/) for more information.

This work was funded by the IMI eTOX prject (http://www.imi.europa.eu/content/etox).
